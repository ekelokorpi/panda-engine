/**
    Particle engine.

    @module particle
    @namespace game
**/
game.module(
    'engine.particle',
    '1.0.0'
)
.body(function() {

game.Particle = game.Class.extend({
    position: null,

    init: function() {

    }
});

game.Emitter = game.Class.extend({

});

});